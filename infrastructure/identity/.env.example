# Generate once with: openssl rand -hex 32
KRATOS_COOKIE_SECRET=replace-with-cookie-secret

# Generate once with: openssl rand -hex 16
KRATOS_CIPHER_SECRET=replace-with-cipher-secret

# Generate once with: openssl rand -hex 32
HYDRA_SYSTEM_SECRET=replace-with-hydra-system-secret
